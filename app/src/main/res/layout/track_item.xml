<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="61dp"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/track_image"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_marginStart="13dp"
        android:layout_marginTop="8dp"
        android:contentDescription="@string/album_picture"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/track_name"
            style="@style/SearchTrackNameText"
            android:layout_width="match_parent"
            android:layout_height="19dp"
            android:layout_marginTop="14dp"
            android:ellipsize="end"
            android:maxLines="1" />

        <LinearLayout

            android:layout_width="match_parent"
            android:layout_height="13dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/track_artist_name"
                style="@style/SearchTrackArtistText"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:ellipsize="end"
                android:maxWidth="150dp"
                android:maxLines="1" />

            <ImageView
                style="@style/SearchDelimeterImageView"
                android:contentDescription="delimeter"
                android:src="@drawable/ic_artist_time_delimeter" />

            <TextView
                android:id="@+id/track_duration"
                style="@style/SearchTrackArtistText"
                android:layout_width="wrap_content"
                android:layout_height="match_parent" />
        </LinearLayout>

    </LinearLayout>

    <ImageView
        style="@style/SearchDelimeterImageView"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="18dp"
        android:layout_marginEnd="12dp"
        android:contentDescription="@string/arrow_forward"
        android:src="@drawable/ic_arrow_forward_track" />

</LinearLayout>